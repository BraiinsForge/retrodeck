diff --git a/bmc-openwrt/src/compositor/render/egl_context.rs b/bmc-openwrt/src/compositor/render/egl_context.rs
index 04f1c22f..6de72933 100644
--- a/bmc-openwrt/src/compositor/render/egl_context.rs
+++ b/bmc-openwrt/src/compositor/render/egl_context.rs
@@ -9,3 +9,3 @@ use smithay::{
         egl::{EGLContext, EGLDisplay},
-        renderer::{Renderer, gles::GlesRenderer},
+        renderer::{Renderer, TextureFilter, gles::GlesRenderer},
     },
@@ -42,2 +42,8 @@ impl EglContext {
             unsafe { GlesRenderer::new(egl_context) }.context("Failed to create GLES renderer")?;
+        renderer
+            .downscale_filter(TextureFilter::Nearest)
+            .context("Failed to select nearest-neighbor downscaling")?;
+        renderer
+            .upscale_filter(TextureFilter::Nearest)
+            .context("Failed to select nearest-neighbor upscaling")?;
         tracing::info!("EGL context initialized");

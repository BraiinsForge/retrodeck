# ROM library

Game images are owner-supplied local data and are not tracked. The deployer
copies any images under `roms/<system>/` to `/mnt/data/roms/<system>/`.

Save, RTC, state, and emulator configuration sidecars stay beside their
corresponding image on the Deck.
